# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyarrow>=14.0          # parquet panels

# Factor programs & search
polars>=1.0           # the paper's factor programs are Polars-based
optuna>=4.0           # Bayesian (TPE) micro-search
lightgbm>=4.0         # multi-factor model (needs libomp on macOS: `brew install libomp`)
scikit-learn>=1.3,<1.6  # <1.6 for gplearn 0.4.2 compat (sklearn 1.6 broke its tag API)
gplearn>=0.4          # optional GPLearn baseline arm (scripts/04_evaluate.py --gplearn)

# Reporting
matplotlib>=3.7
jinja2>=3.0

# Optional — only needed to run live LLM macro-mutation backends
anthropic>=0.40       # requires a real ANTHROPIC_API_KEY
requests[socks]>=2.32 # Kimi/Moonshot China access via HTTP/SOCKS proxy if needed

# Optional — real data instead of the synthetic panel
# pyqlib                # Qlib A-share OHLCV (CSI300/CSI500); large region-gated download
